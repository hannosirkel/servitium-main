---
apiVersion: v1
kind: Service
metadata:
  name: servitium
spec:
  selector:
    app.kubernetes.io/name: servitium
    app.kubernetes.io/instance: test
  ports:
    - name: http
      port: 8098
      protocol: TCP
      targetPort: http
