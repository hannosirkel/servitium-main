---
apiVersion: v1
kind: Service
metadata:
  name: servitium
  labels:
    app.kubernetes.io/name: servitium
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: servitium
  ports:
    - name: http
      port: 8099
      protocol: TCP
      targetPort: http
